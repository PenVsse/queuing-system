import { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        colorPrimary: '#FF9138'
    },
    components: {
        Table: {
            colorTextHeading: '#fff',
            fontFamily: 'Nunito'
        }
    }
}

export default theme;